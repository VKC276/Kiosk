{
  "name": "vkc-kiosk-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply vkc-kiosk --local",
    "migrate:remote": "wrangler d1 migrations apply vkc-kiosk --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.34.0"
  }
}
